---

- hosts: all
  become: true
  tasks:

  - name: Update distro repo index and install apache and php
    package:
      name:
        - "{{ apache_package }}"
        - "{{ php_package }}"
      state: latest
      update_cache: yes